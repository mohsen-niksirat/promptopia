# build workspace & raw export (keep out of the repo)
work/
tools/node_modules/
package-lock.json
*.log

# Freebuff app local state
.freebuff/

# OS noise
Thumbs.db
.DS_Store
serve.cjs

# raw data dumps (too big for the repo)
*.zip
